# NestJS Template

## Changelog

1. Update eslint
   - Use new config format
   - Use recommended config for typescript
2. Update prettier
   - Use tabs to enable user specific indentation width
3. Update tsconfig
4. Update infrastructure.yaml
   - Use `docker compose` instead of `docker-compose`
5. Migrate from jest to vitest
   - Use v8 coverage
